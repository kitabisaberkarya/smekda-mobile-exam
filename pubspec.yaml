name: smekda_mobile_test
description: SMEKDA MOBILE TEST - Aplikasi Ujian Digital SMKN 2 Sigli
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_windowmanager: ^0.2.0
  flutter_inappwebview: ^6.0.0
  wakelock_plus: ^1.1.4
  connectivity_plus: ^5.0.0
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
